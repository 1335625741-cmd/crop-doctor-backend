flask>=2.0
flask-cors>=3.0
# 微信登录时解密 encryptedData(用 session_key 解密,需要 AES-256-CBC)
# 可选:不装也能跑,只是 encryptedData 解密失败时降级用前端明文 userInfo
cryptography>=3.0
# 生产 WSGI 服务器(部署到 Hugging Face Spaces / Docker 时用)
gunicorn>=21.0
# 调智谱 GLM-4V API(Render 上跑真 AI)
requests>=2.28

# API 限流(诊断/反馈/登录,防刷量刷智谱)
flask-limiter>=3.5

# 数据库(2.2.0 起,生产用 MySQL;本地开发不配环境变量自动 fallback 到 SQLite)
PyMySQL>=1.1.0
# cryptography 已经被 PyMySQL 需要(AES 解密等),但单独列出来更清楚

